# baostock-runner 后续开发最终建议

日期：2026-09-24  
用途：综合当前仓库代码分析与用户提供的《数据采集最大化建议与实施计划》，作为后续开发、拆分任务和验收的依据。本文提出开发决策，不代表这些功能已实现或生产验证已完成。

## 1. 总体结论

项目应发展为“可持续更新、可解释覆盖情况、可复现研究结果的 A 股数据服务”。先修可靠性，再提高采集效率，随后贯通查询与控制台，最后形成研究闭环。

推荐顺序：

**可靠性修复 → 全市场单日批量接口验证 → 标准数据与复权验证 → 查询/控制台闭环 → 有范围约束的选股研究 → 历史扩容及其他数据源。**

继续保留单实例、BaoStock 单 worker/单连接、优先级队列和 SQLite。近期没有必要同时引入微服务、分布式调度或替换数据库。

## 2. 证据范围与可信程度

| 内容 | 本次证据 | 如何使用 |
|---|---|---|
| 日历覆盖、财务空期、缓存、字段读取、队列和控制台缺口 | 当前仓库静态代码 | 可据此建立修复任务，补测试确认边界 |
| 572,872 条日线、300 只证券、近期仅 2～5 只覆盖、容器版本 0.9.3 等 | 用户附件中的生产检查记录 | 作为排查线索；本次没有独立访问生产数据库或核实容器版本 |
| 日历最大日期导致近期缺数 | 代码机制与附件现象吻合 | 高优先级假设；仍需核对生产配置、日历、作业记录与日志 |
| 指定日期 A 股、ETF 和复权因子数据能力 | BaoStock 官方首页列出了这些能力 | 值得尽早实验；具体签名、权限、历史范围、字段、完整性需按实际部署版本验证 |
| 复权计算口径 | BaoStock 官方复权说明及计算示例 | 以官方示例为起点做契约测试和跨除权日对账 |
| 精确发布时间、最新软件版本、北交所及历史成分覆盖 | 附件有陈述，本次未逐项独立验证 | 不写成不可变业务规则；上线前记录核验结果 |

本次未运行生产抓取或数据库迁移。上一轮环境中未找到 Python/Python3，现有测试未执行，因此不能把静态判断表述为测试通过。

## 3. 两份建议的一致之处

1. 数据质量优先于扩大采集量。已有记录不等于请求范围完整，任务完成也不等于数据已更新。
2. 交易日历必须检查覆盖终点；日线不能仅依靠 `MAX(bar_date)` 判断完整性。
3. 财务空报告期必须允许重新检查，近期报告和修订数据应有刷新策略。
4. 日线重要字段需要进入可查询的标准层，不能仅藏在 `raw_json` 中。
5. 证券主数据、采集范围和历史股票池应分开；不能用今天的成分股解释所有历史时期。
6. 研究需要公告时间、数据来源、原始响应及计算版本，才能解释和复现结论。
7. 多源扩展应以字段、单位和语义标准化为前提，渐进落地。

两份建议的侧重点互补：原建议更关注查询层、缓存、运维与控制台落地；附件更关注采集效率、复权、历史股票池和量化研究闭环。

## 4. 分歧、遗漏与最终取舍

| 议题 | 对照结果 | 最终决定 |
|---|---|---|
| 全市场批量采集的时机 | 原建议较晚扩市场；附件主张尽早验证按日全市场接口 | 接受附件的效率方向：可靠性修复后立即验证，也可提前编写离线契约测试；验证通过才切换每日主采集 |
| ETF 优先级 | 附件最后列出的首批顺序把 ETF 放在复权前，但其主体计划将 ETF 放在较后阶段 | 以 A 股研究目标为准，ETF 后置到核心链路验收之后 |
| 复权因子连乘 | 附件把 `backAdjustFactor` 定义为单次事件因子并建议连乘 | 不采纳这一默认假设；官方示例按日期找到适用因子直接乘原价，详见第 7 节 |
| 前复权禁止持久化 | 附件要求前复权按需远端抓取且不入持久库 | 放宽为“不能作为无版本事实覆盖历史”；允许带基准日和因子版本的派生缓存、本地计算或审计快照 |
| 后复权与实际总收益 | 附件倾向用后复权支持长期收益分析 | 连续价格研究与真实账户收益分开；真实收益另建分红、配股、持仓和现金流规则 |
| 历史快照与消除偏差 | 附件认为快照能消除幸存者偏差，并辅助判断 ST | 快照只能覆盖实际记录或可靠重建的日期；代码存在/可交易状态不等于 ST 状态，不宣称自动消除全部偏差 |
| 候选池全量留档 | 附件要求候选池保存包括未选中在内的全量结果 | 全量结果存 `screen_result`；`candidate_pool` 仅作为命中结果的视图，必要时另存工作流状态 |
| ETF 必须单独行情表 | 附件要求独立股票池和行情表 | 必须区分资产类型、字段与校验规则；物理共表或分表由语义决定，不强制复制相同 OHLCV 表 |
| 所有重要字段都建索引 | 附件倾向提升字段并建索引 | 提升常用字段，索引依据实际查询和执行计划添加，避免无效写入成本 |
| 实施清单的完整性 | 附件缺少查询层统一、控制台后端、缓存 TTL、请求背压等任务 | 保留原建议这些任务，作为可研究 MVP 的前置能力 |

## 5. 第一阶段：恢复可靠的每日更新（P0）

### 5.1 日历与目标交易日

- 业务交易日采用 `Asia/Shanghai`；采集时间戳统一保存带时区时间或 UTC。预算日界线单独明确，不混用系统本地日期和 UTC 日期。
- 按日历覆盖终点触发补齐。若上游支持未来日历，可预拉一个窗口；否则按日补齐，不假设未来日期必定可得。
- 区分“今天是交易日”“目标数据预计可用”“目标数据已验证完整”。盘前或收盘前不能把空结果记为当天永久完成。
- 上游预计更新时间只作为启动检查的参考，任务必须能延迟重试，最终以结果校验决定是否完成。

### 5.2 日线覆盖与字段完整性

- 以证券、交易日、数据集/价格口径判断缺口，处理历史头部、中间区间和尾部。
- 应覆盖集合由指定日期的研究范围与证券状态决定。未上市、已退市、停牌、上游不支持、尚未更新、未知缺失分别记录；停牌合法零成交不得被当作异常价格。
- 新采集以不复权为基础口径；兼容已有复权记录，但不能用第一种复权的完成状态代表其他口径完成。
- 不必为所有证券×所有历史日永久预建任务行；按日期覆盖汇总、缺口明细及连续区间生成修复批次。
- 优先补可查询字段 `preclose`、`tradestatus`、`isST` 和现有估值字段。区分“不提供”“缺失”“合法为空”。
- 收窄字段的查询不得清空已有完整记录；使用统一采集字段或明确的字段级合并规则。
- `force_refresh` 必须能越过事实表覆盖判断；当前 `use_cache=False` 只绕过参数缓存，不等于强制刷新已覆盖区间。

### 5.3 可重查的任务状态

- 作业至少区分 `pending/running/succeeded/waiting_data/retryable_failed/permanent_failed`，并记录请求窗口、重试次数、下次执行时间、错误分类和行数。
- 财务空期进入 `waiting_data`，按合理披露窗口复查；已采集的近期报告也需检查修订。
- 分红和复权因子不能因为曾经完成过一次而永远跳过，改为按事件日期/检查窗口增量更新。
- 数据提交与任务成功状态尽可能同事务完成；进程重启后回收过期 `running` 任务。
- 修复复权字段映射时同时检查 fetcher 的过滤逻辑和 storage 的日期键，两处当前都依赖 `date`；按真实响应把字段标准化后再存储。

### 5.4 缓存、预算与运行保护

- 参数缓存按数据集设置 TTL，空结果单独配置短期有效期。过期本地结果可按策略返回，但必须标记过期状态。
- 本地读取不应强依赖远端登录成功。故障或熔断时，允许读取已有数据并报告远端不可用。
- 在 worker 实际发起远端调用的位置统计总量、MCP、后台、失败及重试，保留后台预算上限与前台容量；应用自设预算不冒充上游官方配额。
- 调整状态接口：当前 `usage` 包含后台请求，不能直接命名为 `mcp_used_today`。
- 增加队列上限、调用期限和过期任务清理；调用方超时不代表底层线程已停止，底层卡死继续由现有看门狗兜底。
- 失败作业落库并退避，不让一个错误代码永久占据队首；保留前台优先的同时防止后台长期饥饿。
- 迁移前做可恢复备份，增加 schema 版本；补健康检查、日志轮转和基础告警。

### 验收门槛

1. 指定交易日的应覆盖数、有效覆盖数、缺失数及原因可查询，覆盖不完整时不返回“全部追平”。
2. 自动化测试覆盖：跨月/同月日历推进、盘前空结果后补齐、历史区间缺口、空财报后发布、窄字段刷新不丢字段、重复运行幂等。
3. 至少连续三个交易日验证收盘后自动更新，并保存覆盖报告；另以测试覆盖周末、停牌和重启场景。
4. 备份在隔离路径成功恢复，迁移后核心行数、日期范围及关键字段可对账。

## 6. 第二阶段：验证并接入按日期批量采集（P1）

优先验证附件提出的 `query_daily_history_k_AStock(date)`；实际方法名、签名和可用性以固定部署版本为准。

实验必须通过现有唯一 Gateway 执行，不另启动直连 BaoStock 的实验 worker。先记录运行版本、方法签名、字段和错误码，再选单日样本；扩展到普通交易日、近期日期和包含特殊证券状态的样本后，才做切换决策。

实验产物至少包括：

- 原始响应、实际列名、数据类型、单位、价格口径和空值语义。
- 与同日证券集合的差集，区分股票、指数、ETF；明确北交所、停牌和 ST 覆盖边界。
- 估值字段是否存在、是否完整；缺失时明确补充来源和覆盖率，不将行情完整等同于可选股。
- 抽样与逐股历史接口对账；记录误差、行数、耗时、内存和结果读取是否完整结束。
- 历史可查询范围、权限、限额行为及延迟发布表现。

通过后采用“双路径”：

- 每日主采集：日期 → 批量接口 → 原始记录 → 标准化 → 质量校验 → 批量落库。
- 历史回填/补漏：代码与日期窗口 → 逐股接口 → 同一标准化与校验流程。

批量接口失效或不支持某市场时，明确降级范围，保留旧路径。大批次检查行数保护、持久化时间与队列延迟，不盲目提高上限。

全市场每日采集可在实验通过后先运行；全市场长历史和全量财务仍分阶段回填，不能让长期任务挤占每日更新。

## 7. 第三阶段：标准数据与复权验证（P1）

### 7.1 最小标准层

先完成证券标识、交易日、日线、日估值/交易状态、财务指标、复权事件及数据覆盖记录。保留原始响应、来源、采集时间和 schema 版本。

统一数据架构方案可作为蓝图，但不要一次迁移全部表。通过兼容查询或可回滚迁移逐步替换，既有 MCP 客户端在迁移期间保持可用。

### 7.2 修正复权因子理解

BaoStock 官方说明的后复权计算示例，是找到小于等于行情日期的适用后复权因子，再乘以不复权价格：

`adjusted_price(t) = raw_price(t) × applicable_factor(t)`

因此，**不能把接口返回的 `backAdjustFactor` 默认当成独立单次事件倍率再逐项连乘**。必须确认不同接口返回的是累计适用因子还是单次变化比例，并区分 `backAdjustFactor`、`foreAdjustFactor`、`adjustFactor` 的含义。

官方 PDF 示例：2017-05-24 开盘价 15.38 使用适用因子 7.128788，得到 109.64076；并没有把各历史返回行再次相乘。文档个别文字有笔误，需连同表格、公式与实际接口对账理解。

来源：[BaoStock 官方复权因子说明](https://www.baostock.com/helpdocs/pdf/BaoStock%E5%A4%8D%E6%9D%83%E5%9B%A0%E5%AD%90%E7%AE%80%E4%BB%8B.pdf)。

### 7.3 最终复权存储决策

- 不复权行情作为基础事实；因子作为带生效日、来源与采集版本的记录。
- 前/后复权序列作为派生结果，可缓存；缓存键包含价格口径、基准日、因子版本和算法版本。
- 不要求前复权每次都远端抓取，也不禁止保存审计快照；禁止无版本地覆盖旧研究所用序列。
- 后复权也要支持上游修订与重新计算，不能假定永不变化。
- 查询窗口起点之前的适用因子必须可获得；不能因为窗口内没有事件就默认因子为 1。
- 样本涵盖多次除权、区间从事件中间开始、无事件以及适用的配股等情形，对照官方前/后复权返回。预先约定误差容限并保存差异报告。
- 本地算法未通过时，使用带来源和版本的官方复权结果作为临时路径，不阻塞基础行情服务。

复权连续价格与实际总收益分开。官方说明指出，其涨跌幅复权口径不能直接代表投资者实际参加分配的收益。真实收益回测应另行定义分红、配股、现金余额、交易费用与成交限制。

## 8. 第四阶段：统一查询与控制台（P2）

建议结构：

```text
MCP / Dashboard HTTP API
          ↓
共享查询服务：参数校验、覆盖/新鲜度判断、结果格式
          ↓
SQLite 标准层 ← 采集调度 → Gateway 唯一 worker → BaoStock
```

- 财务、行业、交易日历等接口优先访问标准事实表，避免后台事实表与 MCP 参数缓存各自更新。
- 返回数据附 `source/data_as_of/fetched_at/stale/coverage`；复权数据附基准日与版本。大结果分页，保留旧接口兼容策略。
- 修正最新快照只查今天、基本资料 status 未实际过滤、财务 dataset 不一致、指定回填参数不实际选择任务等问题。
- 当前前端已有 `/api/dashboard/*` 和 `/api/stocks/*` 调用，但仓库未发现对应后端路由及静态挂载；补齐这些接口，复用共享查询服务。
- 控制台首先显示目标交易日、按范围/字段统计的覆盖率、缺口原因、排队与错误、下次重试和数据更新时间，其次展示 K 线与财务。
- 读取默认本地；刷新动作明确显示请求范围与结果。对外暴露管理操作时增加访问控制，避免匿名触发大量远端任务。
- 验收：同一查询经 MCP 和 HTTP 返回一致的数据与元信息；断开上游后仍可查看本地数据及过期标记。

## 9. 第五阶段：可复现的研究最小闭环（P3）

先做一个明确标注范围的沪深300或自定义研究池 MVP，再扩大策略范围。每日全市场采集与研究范围可以不同。首版包含日频多头组合回测，使用复权价格近似收益并明确标注，不声称模拟真实账户总收益。

### 9.1 股票池与时点信息

- 证券主表表示证券身份；采集配置表示要下载什么；历史股票池表示某时点属于哪个集合，三者分开。
- 股票池记录观察日期；只有上游有证据时才填写成分实际生效日期，不能用抓取日期冒充。
- 从今天保存快照不会自动还原过去。无法可靠获得历史成分时，回测明确限定为固定研究池或已有快照期间，并标注偏差。
- 财务保留报告期、公告日期、采集时间和修订版本。公告日期只有“日”粒度时采用保守可用时点规则。
- “报告期早于回测日”不足以证明当时可见；今天抓到的修订历史数据也不能仅按旧公告日回填为当时已知。
- 历史 ST、停牌及退市状态需相应的历史字段证据，不能只从上市/退市日期或证券列表推断。

### 9.2 最小表与结果关系

| 表/实体 | 用途 |
|---|---|
| `watchlist` | 人工关注，含分组、备注和维护时间 |
| 历史 universe/snapshot | 研究范围在指定日期的成员及来源 |
| `factor_daily` | 指标值，关联数据版本、口径和算法版本 |
| `screen_run` | 一次筛选的日期、股票池、规则、参数、输入版本和质量状态 |
| `screen_result` | 本次全部评估对象的结果，含命中、未命中、排除、数据不足及原因 |
| `candidate_pool` | 优先做命中结果视图；需要人工处置状态时增加关联记录 |

先选择少量可解释指标，例如动量、波动率、换手及盈利/现金流质量。财务采集根据指标所需字段逐步增加，不必首版把所有接口全部开启。

缺字段不得静默当作 0。每次运行记录实际参与范围；质量不足时阻止依赖该字段的规则，或输出明确的部分结果。

验收：能解释某只股票为什么入选/未入选；固定输入版本与规则可重算同一结果；更换模型也不丢失旧运行证据。

具备真实历史范围与可用时点后，再开放可信回测；使用当日收盘数据产生的信号不能假设当日收盘已经成交，另测停牌、涨跌停、费用和执行时点。

### 9.3 日频多头复权近似回测（F-03，本轮计划新增）

首版回测模型固定为**日频多头组合、复权近似收益**，用于工程验收而非真实账户总收益模拟：

- 每周最后一个交易日收盘后生成组合信号，下一交易日开盘尝试调仓。
- 初始模拟资金 100 万；最多 10 只等权，未使用资金留现金；使用同版本后复权 OHLC 与可分割模拟份额，不模拟真实整手股数。
- 先卖后买、不加杠杆、不做空；当天买入不能当天卖出。
- 默认单边综合费用 10 个基点、单边滑点 5 个基点，均为可配置实验假设；运行结果必须展示参数。
- 停牌或缺少有效开盘价不成交；涨跌停边界有可靠证据时开盘触及上限不买、触及下限不卖，边界未知则该证券当日不交易并记录原因。
- 未成交订单最多保留 3 个交易日，新的调仓目标替换旧订单；卖出失败的持仓继续持有，可用现金不足按买入目标比例缩减。
- 不另行入账分红或配股（避免与复权价格重复计算），结果固定标记 `adjusted_price_approximation`。
- 持仓缺少有效估值时沿用最后有效价格并标记陈旧；超过 3 个交易日的非已知停牌缺失使运行变为 `incomplete`，不输出完整绩效结论。
- 输出订单、成交、每日持仓、现金、净值、累计/年化收益、最大回撤、换手及费用，不增加未经定义的风险评分。

验收：信号不得当日成交；停牌/涨跌停/缺数据/现金不足/未成交订单处理符合上述规则；费用可手算对账；同版本重跑结果一致。

（详细任务拆解见第 14 节任务 F-03。）

## 10. 后续扩展与暂缓范围（P4）

按业务价值选择，避免把所有功能变成首版依赖：

1. 主要指数行情可随研究 MVP 作为基准接入；行业聚合、市场广度在覆盖充分后增加，并明确分母及市场范围。
2. 扩全市场长历史和财务，分“近期优先、研究池优先、长历史渐进”；先评估时间、存储与每日预算。
3. 业绩预告、快报建立事件层，保留公告/观察时间和修订关系。
4. ETF 独立资产类型、采集与校验规则，按需求决定是否独立事实表；不靠交易所前缀判别资产类型。
5. 分钟线只采 watchlist 与近期候选的有限窗口，单独时间戳模型和保留策略，避免全市场长期分钟库。
6. Tushare 等增强源按具体缺失数据接入，保留各源记录，做差异对账后再选择标准推荐值。
7. 新闻与模型辅助解释后置；解释引用已保存数据和筛选证据，不让语言模型输出替代事实校验。

宏观接口、全市场分钟线、多源全面迁移和复杂模型不进入近期版本。

## 11. 建议的开发批次与合并门槛

| 批次 | 交付 | 合并/上线依据 |
|---|---|---|
| A | 日历、目标交易日、缺口、空期重查、字段与缓存修复 | 回归测试通过；生产只读基线与覆盖报告可对比 |
| B | 作业状态、请求计数、期限、备份及迁移机制 | 故障/重启/恢复测试通过，失败可追踪 |
| C | 按日批量接口 Adapter 与实验报告 | 固定版本契约通过，覆盖及抽样对账通过，具备回退 |
| D | 标准字段、因子日期映射、复权及版本缓存 | 多事件与跨窗口样本对账通过，迁移可回滚 |
| E | 共享查询服务、MCP 语义修复、控制台后端 | 两入口结果一致，本地离线读取与分页正常 |
| F | 有范围约束的因子/筛选与日频回测 MVP | 全量评估留档、结论可解释、回测可复现且标记复权近似口径 |

第一轮（批次 A）增加两个前置条件：
- **ENV-01 测试环境**：建立 Python 3.12 隔离测试环境，区分测试/生产启动入口，锁定依赖版本，纠正 README 测试目录，让 `python -m unittest discover -s tests -v` 可执行；测试不登录真实 BaoStock、不访问生产数据库。
- **B-01 迁移基础设施**：引入 schema 版本与顺序迁移，SQLite 一致性备份与隔离恢复，旧结构样本迁移两次一致；迁移前完成备份恢复演练。

基础运维随 A/B 推进；C 的实验准备可与 A/B 交错进行，但批量采集正式切换依赖质量校验与回退能力。D 的字段规范应在 C 写生产数据前明确。以门槛推进，不根据未经测量的工作量承诺固定天数。

## 12. 开工检查清单

- [ ] 在现有生产实例内或通过其受控入口读取版本和只读数据库基线，不启动第二个直连 BaoStock 的进程。
- [ ] 固定已验证的依赖版本；当前 `baostock>=0.8.9` 不能保证每次构建行为一致。
- [ ] 记录生产日历终点、各交易日覆盖、财务空任务、复权原始字段和失败作业。
- [ ] 建立可用测试环境，运行现有测试；检查测试是否与当前看门狗“仅 busy 时判定”的语义一致。
- [ ] 为日历推进、空期后发布、历史缺口和字段合并建立回归测试。
- [ ] 在任何表结构迁移前完成备份恢复演练。

下一轮最合理的具体任务是：**完成批次 A，并提交 C 的接口实验报告**。ETF、新模型和全面多源迁移不作为这一轮的交付目标。

## 13. 来源

- 用户附件：《baostock-runner 数据采集最大化建议与实施计划》，2026-09-24；生产统计日期为附件所述的 2026-09-23，本次未重新实测。
- 当前项目：`baostock_runner/{gateway,fetcher,storage,server,config}.py`、`dashboard/`、测试、Docker 配置及统一数据架构方案。
- [BaoStock 官方首页](https://www.baostock.com/)：列出指定日期 A 股、ETF 日 K 与复权因子数据能力；具体版本行为仍需实验验证。
- [BaoStock 官方复权因子说明](https://www.baostock.com/helpdocs/pdf/BaoStock%E5%A4%8D%E6%9D%83%E5%9B%A0%E5%AD%90%E7%AE%80%E4%BB%8B.pdf)：复权计算示例及收益口径边界。

## 14. 本执行计划与 Agent 交接

完整独立文档：[baostock-runner 分步执行与 Agent 交接计划](baostock-runner分步执行与Agent交接计划.md)。
本节是执行计划的落地记录，与本仓库、任务状态（第 15 节）和决策变更（第 16 节）保持一致。

### 14.1 执行范围与边界

- 默认完成代码、测试、隔离数据库验证及部署说明。
- 生产访问、远端实验、迁移上线和连续交易日观察单独验收（EXT 系列任务），不因代码完成而自动执行。
- ETF、分钟线、多数据源全面接入、新闻和复杂模型不在本轮范围。

### 14.2 执行顺序与任务清单

默认串行执行，后续 agent 每次领取一个任务；每个任务均含实现、相关测试、文档状态更新。

```text
DOC-01 → ENV-01 → B-01 → A-01 → A-02 → A-03 → B-02 → A-04 → C-01 → D-01
       → C-02 → D-02 → E-01 → E-02 → F-01 → F-02 → F-03 → F-04
```

| ID | 依赖 | 交付与验收摘要 |
|---|---|---|
| DOC-01 | — | 建立执行与交接记录（本节 + 第 15/16 节；修订第 9/11 节） |
| ENV-01 | — | Python 3.12 隔离测试环境；区分测试/生产启动入口；锁定依赖版本；纠正 README 测试目录；`unittest discover -s tests -v` 可执行 |
| B-01 | — | schema 版本及顺序迁移；SQLite 一致性备份与隔离恢复；旧结构样本迁移两次一致；未知更高版本拒绝写入 |
| A-01 | B-01 | 统一时间（Asia/Shanghai 业务日 + UTC 审计）与日历推进；可配置当日检查时间；旧预算日保留语义 |
| A-02 | A-01 | 日线字段提升（preclose/tradestatus/isST/peTTM/pbMRQ/psTTM/pcfNcfTTM）+ 缺口覆盖检查 + force_refresh + 覆盖状态区分 |
| A-03 | A-02 | 任务状态机（pending/running/succeeded/waiting_data/retryable_failed/permanent_failed）+ 财务修订重查 + 指数退避 + 同事务提交 + 重启回收 |
| B-02 | A-03 | 请求计数、期限与基础运维（origin/request_id/deadline、总预算与后台比例、队列容量/期限/清理） |
| A-04 | A-03、B-02 | 缓存 TTL 分类型、过期本地结果标记 stale、远端故障时仍可读本地 |
| C-01 | A-04 | 按日批量接口 Adapter 与实验工具（固定版本、签名、原始响应、证券差集、性能报告） |
| D-01 | B-01、A-02 | 最小标准模型与版本留存（资产类型、来源、原始批次、版本记录） |
| C-02 | C-01、D-01 | 接入每日主采集 daily_batch，历史回填走逐股路径，批量校验后提交 |
| D-02 | D-01、C-01 | 复权因子日期映射为 effective_date、累计因子直接相乘、版本化缓存 |
| E-01 | A-04、D-01 | 共享查询服务与 MCP 兼容（metadata、游标分页、强制刷新受控任务） |
| E-02 | E-01、B-02 | HTTP 路由 /dashboard /api/* 与健康检查、令牌鉴权 |
| F-01 | D-01、E-01 | 研究股票池与时点证据（watchlist、universe 快照、point_in_time/revised_history） |
| F-02 | F-01、D-02 | 因子（动量/波动率/换手/roeAvg/CFOToNP）、screen_run/screen_result/candidate_pool 全量留档 |
| F-03 | F-02 | 日频多头复权近似回测（见第 9.3 节） |
| F-04 | F-03、E-02 | 研究入口与结果展示、job_id 异步任务、导出 |

### 14.3 公共接口与数据约定

- 查询元信息采用 `metadata`：`source`、`data_as_of`、`fetched_at`、`stale`、`coverage`；复权和研究结果另带版本。
- `coverage` 至少含范围标识、目标交易日、应覆盖数、有效数、缺失数、原因汇总和 `complete/partial/unknown`。
- 旧接口字段保持兼容，新增结构仅追加；确需破坏兼容时另开任务。
- HTTP 参数错误 400、未授权 401、队列已满 429、本地服务不可用 503；可用但不完整返回 200 并明确标记。
- `start_backfill(dataset)` 保留工具名，返回实际任务标识；长任务统一用作业状态。
- 事实数据、原始响应、作业状态和研究产物分别保存；禁止日志或导出包含凭据。

### 14.4 验收、生产门槛与回滚

阶段验收固定门槛（每任务必须通过相关单元测试；表结构增加迁移测试，接口增加契约测试，前端增加类型检查与构建）：

| 门槛 | 必须证明 |
|---|---|
| A/B 隔离验收 | 日历推进、空期重试、历史缺口、字段合并、预算、队列和恢复行为正确 |
| C 隔离验收 | 固定响应可标准化，部分响应与失败可降级，不误报整日完成 |
| D 隔离验收 | 版本可追溯，复权样本一致，历史研究输入可还原 |
| E 隔离验收 | MCP/HTTP 一致、离线本地读取可用、鉴权和刷新任务可用 |
| F 隔离验收 | 全量筛选留档、时点限制有效、回测可复现且标明近似口径 |

生产步骤单独编号：EXT-01 生产只读基线、EXT-02 远端契约实验、EXT-03 上线与恢复准备、EXT-04 三个交易日观察。
EXT 任务缺少访问条件时标记阻塞，其他离线任务继续。回滚采用停止写入后恢复备份与匹配镜像，不默认执行破坏性逆向迁移。

### 14.5 后续 Agent 执行规则

1. 先读本文件、执行计划和最近交接记录，再核对工作区，防止重复或覆盖已有工作。
2. 领取一个依赖已满足的任务，不跨任务扩展功能；真实接口未验证时只完成离线部分。
3. 不启动第二个直连 BaoStock 的进程；测试使用模拟响应与临时数据库。
4. 修改实现时同步更新受影响测试，保留测试验证的业务意图。
5. 完成后记录实际测试结果、剩余限制、外部验收状态和下一任务。
6. 未执行的测试、实验和观察必须写"未执行"，不能用静态分析替代。
7. 新发现若改变既定决策，先更新第 16 节决策变更记录与受影响任务依赖，再继续实现。
8. 默认不进行生产部署、生产迁移或全市场批量抓取；这些属于独立 EXT 任务。


## 15. 任务状态与验收证据

每个任务使用固定记录格式，`implemented` 表示代码完成，`verified` 表示隔离验收完成；
生产证据单独记录（EXT），不能混为同一种完成状态。

| 字段 | 内容 |
|---|---|
| 任务 ID、依赖 | 对应第 14.2 节任务及前置任务 |
| 状态 | `todo / doing / blocked / implemented / verified` |
| 改动与版本 | 修改内容、提交号；没有提交则注明工作区变更 |
| 验证证据 | 测试命令、结果、报告路径 |
| 外部验收 | `not_required / pending / passed / failed` |
| 遗留问题 | 未覆盖场景及阻塞原因 |
| 下一任务 | 可以开始的任务 ID |

### 15.1 任务状态表

| 任务 | 依赖 | 状态 | 改动与版本 | 验证证据 | 外部验收 | 遗留问题 | 下一任务 |
|---|---|---|---|---|---|---|---|
| DOC-01 | — | verified | 新增第 14/15/16 节；修订第 9/11 节（见 git log） | 见第 14 节，文档结构校验通过（grep 章节 + 读取抽查） | not_required | 无 | ENV-01 |
| ENV-01 | — | todo | 待实施 | — | not_required | — | B-01 |
| B-01 | — | verified | storage.py 引入 SCHEMA_VERSION/顺序迁移/备份恢复；Dockerfile.test-env 等 | `python -m unittest tests.test_storage_schema -v` 7 用例通过；全量 42 用例通过（2026-09-24 本机 3.11） | not_required | 无 | A-01 |
| A-01 | B-01 | verified | timeutil.py(Clock)+storage预算按上海业务日/保守旧键+fetcher日历按覆盖终点/目标交易日+可配置检查时间 | `python -m unittest tests.test_time_advance -v` 10 用例通过；全量 42 用例通过（2026-09-24 本机 3.11） | not_required | 无 | A-02 |
| A-02 | A-01 | todo | 待实施 | — | not_required | — | A-03 |
| A-03 | A-02 | todo | 待实施 | — | not_required | — | B-02 |
| B-02 | A-03 | todo | 待实施 | — | not_required | — | A-04 |
| A-04 | A-03、B-02 | todo | 待实施 | — | not_required | — | C-01 |
| C-01 | A-04 | todo | 待实施 | — | not_required | — | D-01 |
| D-01 | B-01、A-02 | todo | 待实施 | — | not_required | — | C-02 |
| C-02 | C-01、D-01 | todo | 待实施 | — | not_required | — | D-02 |
| D-02 | D-01、C-01 | todo | 待实施 | — | not_required | — | E-01 |
| E-01 | A-04、D-01 | todo | 待实施 | — | not_required | — | E-02 |
| E-02 | E-01、B-02 | todo | 待实施 | — | not_required | — | F-01 |
| F-01 | D-01、E-01 | todo | 待实施 | — | not_required | — | F-02 |
| F-02 | F-01、D-02 | todo | 待实施 | — | not_required | — | F-03 |
| F-03 | F-02 | todo | 待实施 | — | not_required | — | F-04 |
| F-04 | F-03、E-02 | todo | 待实施 | — | not_required | — | — |
| EXT-01 | 生产环境 | todo | 待实施 | — | pending | — | EXT-02 |
| EXT-02 | EXT-01 | todo | 待实施 | — | pending | — | EXT-03 |
| EXT-03 | EXT-02 | todo | 待实施 | — | pending | — | EXT-04 |
| EXT-04 | EXT-03 | todo | 待实施 | — | pending | — | — |

> 状态由执行 agent 在完成/推进每任务后更新本表对应行；生产（EXT）状态由具备生产访问条件时单独更新。

## 16. 决策变更记录

| 日期 | 变更 | 原因与影响 | 关联任务 |
|---|---|---|---|
| 2026-09-24 | 建立本执行计划与交接记录（第 14/15/16 节） | 将《分步执行计划》落地为仓库内可跟踪的执行与验收记录；批次 A 增加 ENV-01 测试环境与 B-01 迁移基础设施前置条件 | DOC-01 |
| 2026-09-24 | 第五阶段（P3）明确日频多头复权近似回测（第 9.3 节） | 固定首版回测模型为复权近似收益并明确交易假设，作为工程验收样例而非真实账户总收益模拟 | F-03 |
