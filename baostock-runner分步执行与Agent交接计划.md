# baostock-runner 分步执行与 Agent 交接计划

## 一、目标、范围与文档落地

本计划覆盖最终建议中的 A–F 全阶段：可靠性、运行保护、批量采集、标准数据与复权、查询控制台、选股与回测。

根据本轮确认，执行边界为：

- 默认完成代码、测试、隔离数据库验证及部署说明。
- 生产访问、远端实验、迁移上线和连续交易日观察单独验收，不因代码完成而自动执行。
- F 阶段包含**日频多头组合回测**，采用**复权近似收益**，不声称模拟真实账户总收益。
- ETF、分钟线、多数据源全面接入、新闻和复杂模型不在本轮范围。

**DOC-01 已完成（2026-09-24）：本计划已写入《后续开发最终建议.md》第 14 节（本执行计划）、第 15 节（任务状态与验收证据）、第 16 节（决策变更记录），并修订第 9 节（日频复权近似回测）与第 11 节（批次 A 前置条件）。**

### DOC-01：建立执行与交接记录

保留原文第 1–13 节，新增：

- 第 14 节：本执行计划。
- 第 15 节：任务状态与验收证据。
- 第 16 节：决策变更记录。

同步修订原第 9、11 节：F 阶段增加日频复权近似回测；第一轮增加测试环境和迁移基础设施前置条件。

每个任务使用固定记录格式：

| 字段 | 内容 |
|---|---|
| 任务 ID、依赖 | 对应下文任务及前置任务 |
| 状态 | `todo / doing / blocked / implemented / verified` |
| 改动与版本 | 修改内容、提交号；没有提交则注明工作区变更 |
| 验证证据 | 测试命令、结果、报告路径 |
| 外部验收 | `not_required / pending / passed / failed` |
| 遗留问题 | 未覆盖场景及阻塞原因 |
| 下一任务 | 可以开始的任务 ID |

`implemented` 表示代码完成；`verified` 表示隔离验收完成。生产证据单独记录，不能混为同一种完成状态。

---

## 二、执行顺序与任务清单

默认串行执行，后续 agent 每次领取一个任务。各任务均需包含实现、相关测试、文档状态更新，不将测试留到最后补齐。

推荐顺序：

`DOC-01 → ENV-01 → B-01 → A-01 → A-02 → A-03 → B-02 → A-04 → C-01 → D-01 → C-02 → D-02 → E-01 → E-02 → F-01 → F-02 → F-03 → F-04`

### 基础准备

| ID | 实施内容 | 交付与验收 |
|---|---|---|
| ENV-01 | 建立 Python 3.12 隔离测试环境；区分测试和生产启动入口；记录依赖版本并生成精确锁定文件；纠正 README 测试目录 | `python -m unittest discover -s tests -v` 可执行；测试不登录真实 BaoStock、不访问生产数据库；记录现有失败，不通过删除断言掩盖问题 |
| B-01 | 引入 schema 版本及顺序迁移；优先采用新增表/列；增加 SQLite 一致性备份与隔离恢复 | 对旧结构样本迁移两次结果一致；恢复后完整性检查、行数及关键字段对账通过；未知的更高 schema 版本启动时拒绝写入 |

**ENV-01 特别要求：**

- 现有“空财报永久跳过”测试在 A-03 中改为“重试时间前跳过、到期后重新查询”。
- 看门狗测试分别验证空闲不误杀、忙碌停滞触发。
- 用可注入时钟和模拟响应测试日期与重试，避免依赖当前日期、未来年份数据或长时间等待。
- 如执行环境缺少 Python，先完成可复现的开发容器配置；无法运行时保持验收待完成，不写“测试通过”。

### A：数据可靠性

**A-01：统一时间与日历推进。依赖 B-01。**

- 业务日期与新预算计数统一按 `Asia/Shanghai` 划日，审计时间保存 UTC。
- 日历按覆盖终点触发补齐。若上游支持未来日历，可预拉一个窗口；否则按日补齐，不假设未来日期必定可得。
- 日线当日检查默认从上海时间 18:00 开始，时间可配置，仅代表开始尝试。
- 收盘前以最近已结束交易日为目标；空结果不代表当天完成。
- 旧预算日记录保留原语义，切换当日按新旧计数中较保守的已用量限制请求，不重置额度。

**A-02：日线覆盖、字段与强制刷新。依赖 A-01。**

- 提升 `preclose/tradestatus/isST/peTTM/pbMRQ/psTTM/pcfNcfTTM` 为可查询字段。
- 保留原始 JSON；缺省字段不覆盖旧值，明确出现的合法空值按接口语义处理。
- 覆盖检查处理头部、中间和尾部缺口，不再以最大日期代表整个区间完整。
- 将缺口合并为连续请求窗口，不预生成全市场所有历史日期的任务笛卡尔积。
- 新增 `force_refresh`，同时绕过参数缓存和事实表“已覆盖”短路。
- 覆盖结果区分预期证券、有效记录、合法排除、未知缺失；行情与估值字段分别统计。
- 证券历史状态证据不足时返回覆盖状态 `unknown`，不推断为完整。

验收：窄字段请求不丢数据；历史缺口可补；不同复权口径独立判断；停牌合法记录不被当作坏数据。

**A-03：可重查任务与财务修订。依赖 A-02。**

- 建立 `pending/running/succeeded/waiting_data/retryable_failed/permanent_failed` 状态。
- 增加任务窗口、`next_retry_at`、租约到期、请求次数、行数和错误分类。
- 空财报等待 24 小时后重查；未来未结束季度不入队。
- 最近两个已结束季度每天检查一次修订，其余已完成报告每 30 天检查一次；全部可配置。
- 网络失败指数退避，默认 60 秒起、最长 1 小时；预算耗尽等待下一预算日。
- 分红、复权改为按检查窗口增量更新，不再按股票永久完成。
- 成功数据与作业完成状态同事务提交；进程重启回收过期 `running` 任务。
- 迁移旧 `done`：有事实记录则保留完成但允许刷新；空财报、分红与复权按新规则重新评估，避免无条件全量重抓。

验收：空期到期后补到新报告；修订保留旧版本；失败股票不阻塞后续任务；崩溃重启不丢任务、不重复插入事实。

**A-04：缓存有效期与上游故障下的本地读取。依赖 A-03、B-02。**

- 缓存保存方法、创建与过期时间；旧无分类缓存到期失效，不清空事实表。
- 默认 TTL：空结果 15 分钟，近期行情 15 分钟，历史行情 7 天，财务与成分 1 天，基本资料与行业 7 天；配置可覆盖。
- 查询先检查本地可用数据，再判断是否需要远端登录与队列。
- 远端故障时可返回本地结果，并标记 `stale`、覆盖不足及远端错误；无数据时返回明确错误。
- TTL 控制重取时机，事实表覆盖检查仍独立执行。

验收：过期会重查；空结果不会永久命中；登录失败或熔断时仍能读本地；强制刷新确实触发远端。

### B：队列、预算与运维

**B-02：请求计数、期限和基础运维。依赖 A-03。**

- Job 增加 `origin`、请求 ID 和 deadline；来源使用 `mcp/dashboard/fetcher/maintenance`。
- 在实际远端调用前记录一次尝试；失败、重试、登录验证调用分别可追溯。
- 总预算限制全部远端请求，后台受独立比例上限限制；缓存命中不计远端请求。
- 队列默认容量 256；前台请求期限 120 秒，后台 300 秒，均可配置。
- 排队过期任务跳过；执行中调用方超时只停止等待，不能伪称底层 SDK 已取消。
- 默认前台优先；后台等待超过 60 秒时，每处理 10 个前台请求允许一个后台请求，避免永久饥饿。
- 关闭服务时终结未执行请求；保留看门狗处理底层卡死。
- 增加结构化失败日志、日志轮转和健康状态服务，HTTP 路由在 E-02 挂载。

验收：重复重试计数准确、缓存不扣费、后台不超配额、满队列快速失败、关闭不永久挂起、空闲不误杀。

### C：按日期批量采集

**C-01：Adapter 与接口实验工具。依赖 A-04。**

- 只为允许的方法建立类型化 Adapter，不增加任意远端方法执行入口。
- 实验工具使用注入的 Gateway；不得通过导入服务模块意外创建第二个连接。
- 保存固定版本、签名、原始响应、字段映射、权限、日期范围、证券差集和性能报告。
- 离线测试使用固定响应样本；真实实验只能走现有唯一 worker。
- 实验结果状态固定为 `supported/partial/unsupported/unverified`；没有真实访问时必须标记 `unverified`。

真实实验验收样本：三个可取得的已结束交易日，每日至少 20 只分层抽样，与逐股接口比较；所有证券差集必须解释。普通行情数值按源精度比较，默认绝对误差不超过 `1e-6`。

**C-02：接入每日主采集。依赖 C-01、D-01。**

- 新增模式 `per_stock/daily_batch`，默认保留 `per_stock`。
- `daily_batch` 使用日期级任务和覆盖校验；历史回填与缺口继续走逐股路径。
- 批量响应先校验再提交；截断、重复键冲突或未解释缺失不得标记整日完成。
- 缺估值字段时追加补充任务，不能把行情覆盖报告当作筛选就绪报告。
- 接口不可用时退回逐股路径，并保留降级状态。
- 未通过真实实验时仅允许隔离模式验证，不切换生产默认值。

验收：两条路径得到相同标准记录；重复执行幂等；部分响应、权限错误、超时和预算不足均可恢复。

### D：标准数据与复权

**D-01：最小标准模型与版本留存。依赖 B-01、A-02。**

- 保留现有证券代码为兼容标识，增加资产类型与来源；不在本轮强制重建全部统一架构表。
- 标准化日期、数值、金额/数量单位和空值语义，字段映射集中维护。
- 现有事实表保留最新可查询值；增加原始采集批次及发生变化时的版本记录。
- 财务版本保留报告期、公告日期、观察时间与原始字段；研究数据可定位具体版本。
- 历史未知来源和时间标记为未知，不补造精确时间。
- 既有数据以迁移时首次观察时间建立基线，不能宣称迁移前已保存所有修订历史。

验收：旧 MCP 字段兼容；相同响应不产生重复版本；更新可追溯；旧版本可重建。

**D-02：复权映射、计算与版本缓存。依赖 D-01、C-01。**

- 在 Adapter 将实际返回的因子日期映射为统一 `effective_date`，同步移除 fetcher/storage 对原始 `date` 的错误假设。
- 按接口契约区分累计适用因子与单次比例；累计因子按日期匹配后直接乘原价，不再默认连乘。
- 查询起点之前的适用因子必须可得，未知时返回不完整，不自动填 1。
- 缓存键包含证券、日期区间、口径、基准日、行情版本、因子版本和算法版本。
- 对照官方返回，覆盖多次除权、区间从中途开始及无事件样本；预先约定误差容限并保存差异报告。
- 本地算法未通过时，使用带来源和版本的官方复权结果作为临时路径，不阻塞基础行情服务。

来源：[BaoStock 官方复权因子说明](https://www.baostock.com/helpdocs/pdf/BaoStock%E5%A4%8D%E6%9D%83%E5%9B%A0%E5%AD%90%E7%AE%80%E4%BB%8B.pdf)。

### E：统一查询与控制台

**E-01：共享查询服务与 MCP 兼容。依赖 A-04、D-01。**

- MCP 与 HTTP 共用本地查询、覆盖检查、刷新及元信息生成。
- 保留已有返回键，新增统一 `metadata`；不强制一次性改变所有旧返回格式。
- 修正最新快照只查今天、基本资料状态未过滤、财务 dataset 不一致、指定回填参数不选择实际任务的问题。
- 日期使用真实日期解析；字段白名单、证券代码、范围和数据集统一校验。
- 大结果增加游标分页：新 HTTP 默认 500、上限 5000；旧 MCP 默认行为兼容，显式分页时返回 `next_cursor`。
- 强制刷新形成受控任务，查询返回已存在数据及刷新状态，不让 HTTP 长时间等待回填。

验收：MCP 与共享服务对同一请求得到一致记录与元信息；新增参数不破坏旧调用。

**E-02：HTTP、控制台及健康检查。依赖 E-01、B-02。**

- 在同一服务进程挂载 `/dashboard/`、现有前端调用的 `/api/dashboard/*`、`/api/stocks/*`，不另起 Gateway。
- 刷新接口返回 `202 + job_id`；新增任务状态读取接口，前端轮询完成后重读数据。
- 展示覆盖率、缺口原因、最后成功时间、下次重试、队列及预算；请求失败后不能继续显示旧的“在线”状态。
- `/healthz` 检查进程、本地数据库和任务服务；`/readyz` 以本地查询可服务为就绪，上游状态单独报告。
- 新 HTTP 默认要求配置令牌；仅显式开发模式允许本机免鉴权。令牌不写日志、不放 URL，不改变既有 MCP 客户端认证方式。
- 修正搜索与选股请求竞态，避免旧响应覆盖新选择；补 TypeScript 类型检查。

验收：HTTP 契约测试、未授权拒绝、断开上游仍可看本地数据；前端类型检查和构建通过。

### F：选股研究与日频回测

**F-01：研究股票池与时点证据。依赖 D-01、E-01。**

- 建立人工关注列表、历史研究池快照与输入数据快照。
- 默认范围为沪深300；历史成员未知时，只允许显式固定池实验，并标记幸存者偏差。
- 财务公告只有日期时，默认下一交易日可用；只有首次观察证据时不倒填成当时已知。
- 允许两种研究标记：`point_in_time` 与 `revised_history`。证据不足时不得输出前者。
- 研究运行保存原始输入或可恢复版本，而非只保存无法还原的哈希。

验收：历史池不会被当前成分悄悄替代；加入未来数据不会改变此前的严格时点查询。

**F-02：因子、筛选及全量结果。依赖 F-01、D-02。**

首版固定提供：

- 20 日动量：`close(t)/close(t-20)-1`。
- 20 日波动率：20 个日收益率样本标准差乘 `sqrt(252)`。
- 20 日平均换手率。
- 最新可用盈利指标 `roeAvg`。
- 最新可用现金流指标 `CFOToNP`。

量价指标使用同版本后复权价格；停牌日保留但不得成交。缺少窗口、字段或可用时点证据时标记 `insufficient_data`。

默认验收规则 `baseline_v1`：

- 非 ST、当日可交易、20 日动量为正、`roeAvg > 0`、`CFOToNP > 0`。
- 按动量降序、代码升序排序，最多选择 10 只。
- 规则与窗口均保存版本；这只是工程验收样例，不表达投资有效性。

`screen_run` 保存范围、规则、参数、输入版本和质量状态；`screen_result` 保存本次全部评估对象的结果，含命中、未命中、排除、数据不足及原因；`candidate_pool` 优先做命中结果视图，需要人工处置状态时增加关联记录。

验收：能解释某只股票为什么入选/未入选；固定输入版本与规则可重算同一结果；更换模型也不丢失旧运行证据。

具备真实历史范围与可用时点后，再开放可信回测；使用当日收盘数据产生的信号不能假设当日收盘已经成交，另测停牌、涨跌停、费用和执行时点。

**F-03：日频多头复权近似回测。依赖 F-02。**

固定首版模型：

- 每周最后一个交易日收盘后生成组合信号，下一交易日开盘尝试调仓。
- 初始模拟资金 100 万；最多 10 只等权，未使用资金留现金。
- 使用同版本后复权 OHLC 和可分割的模拟份额，不模拟真实整手股数。
- 先卖后买、不加杠杆、不做空；当天买入不能当天卖出。
- 默认单边综合费用 10 个基点、单边滑点 5 个基点，均为可配置实验假设，不宣称实际市场费率；运行结果必须展示参数。
- 停牌或缺少有效开盘价不成交。涨跌停边界有可靠证据时，开盘触及上限不买、触及下限不卖；边界未知则该证券当日不交易并记录原因，不根据代码随意猜测。
- 未成交订单最多保留 3 个交易日；新的调仓目标替换旧订单。卖出失败的持仓继续持有，可用现金不足时按买入目标比例缩减。
- 不另行入账分红或配股，避免与复权价格重复计算；结果固定标记 `adjusted_price_approximation`。
- 持仓缺少有效估值时沿用最后有效价格并标记陈旧；超过 3 个交易日的非已知停牌缺失使运行变为 `incomplete`，不输出完整绩效结论。
- 输出订单、成交、每日持仓、现金、净值、累计收益、年化收益、最大回撤、换手及费用；不增加未经定义的风险评分。

验收：信号不得当日成交；停牌、涨跌停、缺数据、现金不足和未成交订单处理符合上述规则；费用可手算对账；同版本重跑一致。

**F-04：研究入口与结果展示。依赖 F-03、E-02。**

- 增加创建/读取筛选和回测任务接口，长任务返回 `job_id`，用本地研究任务执行器运行，不占 BaoStock worker。
- 展示股票池、输入版本、规则、质量状态、复权近似收益标记、交易假设和原因明细。
- 提供结果导出；缺数据或修订历史实验必须显著标注，不展示为严格历史时点回测。
- 补筛选至回测端到端固定样本，形成可交接的完整演示。

---

## 三、公共接口与数据约定

实施期间统一遵守以下约定，避免各 agent 自行设计不同协议。

- 查询元信息采用 `metadata`，包含 `source`、`data_as_of`、`fetched_at`、`stale`、`coverage`；复权和研究结果另带相关版本。
- `coverage` 至少包含范围标识、目标交易日、应覆盖数、有效数、缺失数、原因汇总和 `complete/partial/unknown` 状态。
- 旧接口字段保持兼容；新增结构仅追加。确需破坏兼容时另开任务，不夹带在普通修复中。
- HTTP 参数错误返回 400，未授权 401，队列已满 429，本地服务不可用 503；可用但不完整的数据返回 200 并明确标记，不伪装成完整结果。
- `start_backfill(dataset)` 保留工具名，返回实际任务标识；强制刷新和研究运行均使用异步任务状态。
- 长任务状态统一采用前述作业状态；领域上的“不完整研究结果”另用质量字段表示，不与运行失败混用。
- 事实数据、原始响应、作业状态和研究产物分别保存；禁止日志或导出包含凭据。
- 上游字段尚未验证时，Adapter 记录不支持/未知；不得凭猜测填值或改单位。

---

## 四、验收、生产门槛与回滚

每个任务必须通过相关单元测试；涉及表结构增加迁移测试，涉及接口增加契约测试，涉及前端增加类型检查与构建。

阶段验收固定为：

| 门槛 | 必须证明 |
|---|---|
| A/B 隔离验收 | 日历推进、空期重试、历史缺口、字段合并、预算、队列和恢复行为正确 |
| C 隔离验收 | 固定响应可标准化，部分响应与失败可降级，不误报整日完成 |
| D 隔离验收 | 版本可追溯，复权样本一致，历史研究输入可还原 |
| E 隔离验收 | MCP/HTTP 一致、离线本地读取可用、鉴权和刷新任务可用 |
| F 隔离验收 | 全量筛选留档、时点限制有效、回测可复现且标明近似口径 |

生产步骤单独编号：

- **EXT-01：生产只读基线。** 记录实际版本、日历终点、逐日覆盖、财务空任务及因子字段；不把附件统计当成新实测。
- **EXT-02：远端契约实验。** 通过唯一 Gateway 获取 C/D 所需样本，保存对账报告。
- **EXT-03：上线与恢复准备。** 完成一致性备份、隔离恢复演练、迁移前后对账；制定旧镜像与备份恢复步骤。
- **EXT-04：三个交易日观察。** 保存每日覆盖、延迟、失败、预算及自动恢复报告；未完成观察不标记生产验收通过。

EXT 任务缺少访问条件时标记阻塞，其他离线任务继续。回滚采用停止写入后恢复备份与匹配镜像，不默认执行破坏性逆向迁移。

---

## 五、后续 Agent 的执行规则

1. 先读最终建议、执行计划和最近交接记录，再核对工作区，防止重复或覆盖已有工作。
2. 领取一个依赖已满足的任务，不跨任务扩展功能；真实接口未验证时只完成离线部分。
3. 不启动第二个直连 BaoStock 的进程；测试使用模拟响应与临时数据库。
4. 修改实现时同步更新受影响测试，保留测试验证的业务意图。
5. 完成后记录实际测试结果、剩余限制、外部验收状态和下一任务。
6. 未执行的测试、实验和观察必须写“未执行”，不能用静态分析替代。
7. 新发现若改变既定决策，先更新决策记录与受影响任务依赖，再继续实现。
8. 默认不进行生产部署、生产迁移或全市场批量抓取；这些属于独立 EXT 任务。
